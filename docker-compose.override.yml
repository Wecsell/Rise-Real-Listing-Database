# Optional local database diagnostics. The port is loopback-only, so
# PostgreSQL is not published to the host network.
services:
  postgres:
    ports:
      - "127.0.0.1:${POSTGRES_HOST_PORT:-5432}:5432"
