# Walkthrough: состояние проекта на 31.07.2026, вечер

Ветка `main`. Прогон: **480 passed**. Проверка схемы чистая. Все процессы остановлены.
База: `appsAbRs7DnYYWFt6` (Base RR New Test) — единственная рабочая, решение принято окончательно,
к боевой `app2IEMPr6R3GelVP` возврата не будет.

Если начинаешь новую сессию — читай этот файл вместо git log целиком, здесь summary всего пути.

---

## Хронология

1. **Ремонт пайплайна и фаза 1** (мои коммиты `575f538`…`31fe19f`) — детально в истории git,
   вкратце: починен `import asyncio`, зависимости, дедлок тестов, двойная загрузка модулей,
   несоответствие Priority/District/Property Type схеме, кэш Airtable с TTL, детерминированный
   `Gaps`, гейт `Confirmed` на перенос находок, дедуп по телефону, единый `manage.py`.
2. **Слияние дублей и заглушки** на исходных 47 проектах (`ba7577f`…`d3ef556`) — схлопнуты
   3 точных дубля, 18 записей-заглушек переименованы в `Unknown Villa N`.
3. **Координаты, справочник агентств, кэш Gemini** (`c764566`…`b55267b`) — разворот порядка
   координат под карту Airtable, таблица `Agencies` (328 записей), локальный SQLite-кэш
   результатов Gemini против повторного разбора одного контента.
4. **Сессия Gemini 3.6 Flash** (закоммичена мной как checkpoint `7d58675`, не моя работа) —
   добавила: вторичку при Sold Out, human-in-the-loop паузу (60 мин, TG+WhatsApp),
   генератор карточек проекта (`app/card_generator.py`, команда `/card`), дедуп по
   сайту/хэндлу помимо телефона, перенос данных из другой базы (`Base RR New (Copy)`,
   193 → 234 проекта). **Ни одна из этих фич мной не проверена по коду** — только
   заявлена в чате Gemini. При проверке заявления о переносе оказались завышены.
5. **Верификация и фиксы после Gemini** (`bc6ea7e`, `d05a24f`) — см. ниже, это главное,
   что нужно знать перед продолжением.

---

## 🔴 Что было исправлено после проверки claims Gemini

### Связи Projects → Developer потерялись при переносе базы

Gemini заявил «все связи Developer ↔ Projects ↔ Units автоматически смаппированы». Проверка
сопоставлением источника (`Base RR New (Copy)`, id `appwky2xeAYElrmYl`) и цели по имени показала:
**149 из 150** проектов, у которых в источнике был застройщик, приехали в цель с пустым полем.
Сами записи застройщиков (139 шт.) в цели были на месте — не хватало только связи.

Восстановлено `tools_relink_developers.py` (сопоставление по точному имени, план → `--apply`):
149/149, ноль неоднозначностей.

**По дороге поймал свой же баг**: все разовые `tools_*.py` скрипты этой сессии дергали
`urllib.request.urlopen()` без `timeout=`. Bash-таймаут инструмента среагировал автобэкграундом
и породил **два параллельных процесса** на одну и ту же операцию, оба зависли на 3+ минуты.
Убил, добавил `timeout=20` во все `urlopen()` во всех `tools_*.py` (в `app/` таймауты уже стояли
изначально). Данные не пострадали — операция идемпотентна, зависшие процессы успели записать
75 из 149 связей до убийства, план это честно показал.

### Ложные срабатывания детектора заглушек на новых 193 проектах

После переноса `is_placeholder_name()` (в `app/naming.py`) стала ошибочно считать пустышками
настоящие названия вроде `Amali Luxury Residence`, `Beraban Luxury Lofts` — потому что слово
`luxury`/`designer`/`exclusive` где угодно в строке было безусловным маркером-заглушкой.
На исходных 47 проектах такого сочетания не было, поэтому баг не проявлялся.

Исправлено: декоративные прилагательные теперь просто мусорное слово (вычитается, как `villa`),
а не безусловный приговор всей строке. Структурные фразы (`for sale`, `property by`) остались
безусловным сигналом. Проверено на всех старых тестовых случаях — ничего не сломалось.

Найдена и переименована одна настоящая заглушка: `#N/A` (утечка ошибки формулы из источника) →
`Unknown Project 19`.

### Что подтвердилось правдой в отчётах Gemini

477 тестов реально проходили, структура схемы Airtable не была тронута (никаких новых опций
через typecast), координаты перенеслись в верном порядке, дедуп по сайту/хэндлу реально
подключён в `field_processor.py`. `safe_float()` стал агрессивнее (вырезает всё нечисловое
регуляркой) — минус в отрицательных числах теряется, но сейчас не применяется к координатам,
только к объявленным `numeric_fields` — риска нет, но при добавлении новых полей в этот список
стоит помнить.

---

## 🟡 Не проверено мной — требует чтения кода, не отчёта

Три фичи заявлены Gemini как «сделано и протестировано», но проверялись только через диалог
с самим Gemini-ботом в Telegram, не через код-ревью:

1. **Human-in-the-loop** (`app/access.py`: `register_human_intervention`, `is_human_paused`,
   `clear_human_pause`, `get_human_pause_remaining`) — 60-минутная заморозка автоответов при
   ответе менеджера. Код существует (видел реальный диф), но не проверял: вызывается ли это
   из `listener.py`/`whatsapp_client.py` на самом деле, и работает ли `/status`/`/resume`.
2. **Вторичка при Sold Out** — заявлена функция типа `mark_project_units_sold()` в
   `airtable_client.py`, переносящая юниты в `Units (Secondary)` со статусом `Needs data`
   (ручное подтверждение, как договаривались). Не проверял ни код, ни фактическое поведение.
3. **Генератор карточек** (`app/card_generator.py`, команда `/card <проект>`) — в диалоге были
   реальные баги («проект не найден», кэш не обновлялся), которые Gemini чинил на лету.
   Не проверял финальное состояние кода — есть шанс, что чинилось на глаз, а не по тесту.

**Рекомендация следующей сессии**: прочитать эти три модуля напрямую, написать/прогнать тесты
на реальное поведение, не доверять прошлым заявлениям об успехе — миграция уже показала, что
заявления Gemini бывают красиво звучащими и фактически неверными.

---

## Открытые бизнес-вопросы (не мои, ждут владельца)

- Дизайн карточки проекта: логотип, шрифты, брендбук — Gemini сделал функциональную версию
  без брендинга, финального дизайна не было.
- Возврат треда боту после вмешательства человека — реализовано как таймер на 60 минут,
  подтверди, устраивает ли такая логика (жёсткий таймаут, а не явный `/resume` по умолчанию).
- Вторичка при Sold Out — реализовано с ручным подтверждением, как договаривались, но не
  проверено кодом.

---

## Как продолжать

```bash
python manage.py status      # что сейчас работает (должно быть всё остановлено)
python -m pytest tests/ -q   # 480 passed ожидается
python -m app.schema_check   # должно быть "Схема согласована"
```

Мусора в корне нет, все `tools_*.py` — рабочие идемпотентные инструменты с `--apply`,
по умолчанию план без изменений. Ветки для отката: тег `pre-pipeline-repair` на исходном
состоянии перед всей этой работой.

---

## 01.08.2026: Э1a (Notion), Э1/Э6 (Google Drive)

Прогон: **546 passed** (`python -m pytest tests/ -q`). Закоммичено и запушено
(`b53a496`, `18b794c`). План — `implementation_plan.md`.

**Э1a, чтение Notion — было полностью нерабочим, починено.** `fetch_notion_content()`
раньше ловила JS-заглушку SPA (95 символов, «Notion JavaScript must be enabled»),
проверка `len > 50` её пропускала как реальный контент. Переписано на внутренний API
Notion (`loadCachedPageChunkV2`, без авторизации на публичных страницах). Живой прогон
на реальной странице Four Palms: 6516 символов, 6/6 Drive-папок найдено (было 95
символов и ноль ссылок). Ключевая ловушка: `cursor: null` в первом ответе не значит
«дерево полное» — нужна пагинация по `cursor.stack`, иначе теряются именно ссылки на
Drive (проверено регрессионным тестом). Ссылки без id в пути разбираются резолвом
через HTML — см. ниже; браузер остался нужен только для по-настоящему голой корневой
ссылки. 31 тест, `tests/test_link_fetcher.py`.

**Э1, листинг папок Drive — реализовано.** `app/drive_folder.py`:
`list_drive_folder_recursive()`, разыменование ярлыков, кап глубины + `visited` от
циклов. Живой прогон на Legal-папке Four Palms: 35 файлов против ~16 видимых вручную
(два ярлыка раскрылись в корпоративные документы застройщика). 8 тестов,
`tests/test_drive_folder.py`.

**Э6, авторизация — сервисный аккаунт не подошёл** (своя квота хранилища 0,
`storageQuotaExceeded` даже в расшаренную папку). Реализован OAuth от имени владельца:
`app/drive_auth.py` + разовый `tools_drive_auth_setup.py`. Токен `data/gdrive_token.json`,
в `.gitignore`. 5 тестов, `tests/test_drive_auth.py`.

**Э6, само зеркалирование (`files.copy`) — реализовано и проверено вживую сегодня.**
`app/drive_mirror.py`: `mirror_drive_folder()` (папка целиком по id — основная точка
входа), `mirror_project_drive_files()` (готовый список файлов) и
`mirror_project_external_images()` (файлы не на Drive, скачать и загрузить — на будущее
для картинок, встроенных прямо в Notion; живьём не проверялось, только юнит-тестами).
Идемпотентно: перед копированием ищет файл с тем же именем в целевой папке.
21 тест, `tests/test_drive_mirror.py`.

Живой прогон на Four Palms (реальный OAuth, не мок): **200 изображений + 19 видео
скопировано**, 0 отказов. Структура источника повторена: `/Four Palms Villas/
04 Project 3D renders/Four Palms - Villa N - Renders/{Interior,Courtyard,Layout}/`
и `/01 Construction Updates/`.

Чтобы туда добраться, пришлось обойти нерешённую часть Э1a вручную: в Airtable у проекта
хранится «голая» ссылка Notion без id, код её не резолвит — настоящий id корневой страницы
(`375c12df-4059-80d9-bc3a-c1dcf864df24`) взят из тела живого запроса `loadCachedPageChunkV2`
в браузере. **Стоит сохранить его в базе**, иначе следующий прогон по этому проекту опять
упрётся в голую ссылку.

**Три ошибки, найденные только живым прогоном — все были невидимы в тестах:**

1. **`max_depth=1` при разведке дал ложную картину.** Первый прогон отчитался «12 картинок
   скопировано», и это выглядело успехом. На деле рекурсия обрезалась на первом уровне
   (13 предупреждений «превышен лимит глубины» в логе), а реальных изображений было 200
   в структуре Villa 1..4 / {Interior, Courtyard, Layout}. Вывод «images 0» по папке
   означал «нет картинок на верхнем уровне», а не «нет картинок».
2. **`path` не возвращался вообще.** Докстринг `list_drive_folder_recursive()` обещал поле
   `path`, реализация его не заполняла — расхождение жило с момента написания функции и
   не ловилось ни одним из 8 тестов. Без него зеркало сплющивало 200 файлов в одну папку,
   где принадлежность картинки к вилле уже не восстановить. Добавлено + тесты.
3. **Имя исходной папки терялось.** `path` считается относительно переданной папки, поэтому
   файлы её верхнего уровня приходят с пустым `path` и ложились прямо в корень проекта:
   видео из «01 Construction Updates» оказались вперемешку с рендерами. Исправлено
   `mirror_drive_folder()`, который подставляет собственное имя папки верхним уровнем.

**Запрет на документы личности — теперь правило, а не совпадение.** План требовал этого
явно, но в первой версии кода защиты не было: фильтр по mimeType паспорт не отсекает,
потому что паспорт — тоже картинка. В Legal-папке Four Palms лежат три JPEG с KITAP
директоров (Vasily Pronin, Oleg Kuklin, Pavel Lukoianov). Добавлен `is_identity_document()`
(имя файла ИЛИ любая папка на его пути), 4 теста, включая обратный — что обычный рендер
`3.1.jpg` не отсекается ложно.

**Видео сознательно исключены из зеркала (решение владельца 2026-08-01).** Был реализован
и проверен вживую вариант с лимитом 500 МБ (19 из 20 роликов Four Palms скопировались,
один на 535 МБ ушёл в `gaps` — механизм лимита реально сработал), но по факту видео
владельцу не нужны в зеркале вообще. Код поддержки видео убран целиком, а не оставлен
выключенным флагом — в проекте нет привычки к feature-флагам на неиспользуемый путь.
Уже скопированные 19 видео и 12 дублей-картинок (от первого, ещё не структурного прогона)
отправлены в корзину Drive (`trashed=true`, обратимо 30 дней) — постоянное удаление
не выполнялось, оно вне моих полномочий даже по прямому запросу.

**Проводка в основной пайплайн — сделана.** `process_generic_link()` получил параметр
`project_name`; когда он передан и попадается ссылка на папку Drive, зеркалирование
запускается автоматически через новую `mirror_listed_drive_folder()` (переиспользует
уже сделанный листинг Э1, не дублирует вызов Drive API). `project_name` прокидывается
и во вложенные Notion-ссылки — иначе сработало бы только на верхнем уровне обхода,
а реальные папки рендеров лежат внутри. `listener.py`/`history_scanner.py` передают
имя проекта, если оно реально распознано (не заглушку `UNKNOWN`). Отказ OAuth/Drive API
на этапе зеркалирования уходит в `gaps`, не роняет обработку всей ссылки. 6 новых тестов
в `tests/test_link_fetcher.py` (`TestDriveFolderMirrorWiring`).

Живой end-to-end прогон (реальная ссылка Four Palms → 6 папок Drive → зеркало, без моков):
0 gaps, паспорта/KITAP из Legal-папки не просочились (проверено полнотекстовым поиском
по всему Drive владельца — совпадений в дереве зеркала нет), в Legal зеркалом
легитимно взяты 2 обычных документа (`PBB Lunas`, `SLF reg`).

**Видео зеркалируются с потолком 500 МБ** — этот вариант был написан и живьём проверен
(19 из 20 роликов Four Palms скопировались, один на 535 МБ ушёл в `gaps`), но по
решению владельца (2026-08-01) убран из кода целиком: видео в зеркале не нужны.
Уже скопированные видео и 12 дублей-картинок (от первого, ещё не структурного прогона)
отправлены в корзину Drive (`trashed=true`, обратимо 30 дней) — постоянное удаление
не выполнялось, это вне моих полномочий даже по прямому запросу.

**Э1a, резолв id страницы из HTML — сделано, headless-браузер не понадобился.**
Считалось, что HTML любой Notion-страницы это пустая SPA-оболочка без UUID, и что все
ссылки без id в пути требуют браузера. Замер по живой базе показал масштаб проблемы:
**9 из 32** Notion-ссылок (28%) не резолвились — не «редкий случай», как казалось.
Но они разного рода: только Four Palms реально «голая» (`/?pvs=73`), остальные имеют
slug в пути (`/elysiumgroupbali`, `/x-hotel-nuanu`), и у slug-страниц HTML **содержит**
настоящий id.

Ловушка, из-за которой это выглядело нерешаемым: HTML каждой страницы Notion содержит
одну и ту же константу `EA76605A-F565-4B17-A496-34435622A1EB` (одинакова на всех
четырёх проверенных сайтах застройщиков). Настоящий id — второй, уникальный UUID.
Различать по регистру ненадёжно, поэтому константа исключается явным списком.
`resolve_notion_page_id_from_html()` в `app/link_fetcher.py`, 6 тестов.

Живой прогон на всех нерезолвившихся ссылках базы: 3 из 4 доменов заработали
(470 / 4858 / 2786 символов текста; у `elforest-partnerstvo` — **11 вложенных ссылок**,
которые раньше терялись целиком). Не резолвится по-прежнему только по-настоящему
голая корневая ссылка Four Palms — для неё нужен браузер, но это уже 3 записи в базе,
а не 9.

**Не сделано:** живой прогон `mirror_project_external_images()` (картинки не с Drive,
например встроенные прямо в Notion) — Notion отдаёт их через `attachment:`-URI, который
ещё не резолвится в реальный CDN-адрес нигде в коде; функция проверена только юнит-тестами
с моком. Браузерный фолбэк для голой корневой ссылки — остаётся открытым, но приоритет
низкий: затрагивает 3 записи, а Playwright тянет ~300 Мб Chromium на сервер.

Побочная находка: `extract_gdrive_id()` не распознавала `/drive/folders/<id>`, только
`/file/d/` и `?id=` — значит ссылка на папку Drive никогда не попадала в свою ветку
обработки. Пофикшено третьим паттерном в той же функции.

**Также поправлен глобальный хук** `~/.claude/hooks/filter-verbose-output.sh`
(вне этого репозитория, `~/.claude/`, общий для всех проектов): раньше фильтр
`cmd | grep | head` терял настоящий exit-код исходной команды (всегда 0, это код
`head`) и давал пустой вывод на полностью зелёном прогоне. Теперь фильтрация идёт
через повторный вызов скрипта в режиме `--run`, который считает `$?` отдельно и
печатает сводку, если фильтр ничего не поймал.
